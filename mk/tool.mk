CURL   = curl -L -o
CF     = clang-format -style=file -i
GITREF = git clone -o gh --depth 1
RUSTUP = $(CAR)/bin/rustup
CARGO  = $(CAR)/bin/cargo
NPM    = /usr/bin/npm
TSC    = $(HOME)/.npm/bin/tsc
YO     = $(HOME)/.npm/bin/yo
